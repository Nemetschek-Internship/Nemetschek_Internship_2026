@model Entities.ViewModels.Auth.SetPasswordViewModel

@{
    Layout = null;
    ViewData["Title"] = "Set Password";
    var formAction = ViewData["FormAction"]?.ToString() ?? "SetPassword";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NemeBook — Create a new password</title>
    <style>
        :root {
            --orange: #F97316;
            --orange-dark: #EA580C;
            --ink: #1F2937;
            --muted: #6B7280;
            --border: #E5E7EB;
            --bg: #F9FAFB;
            --strong-green: #16A34A;
            --strength-weak: #EF4444;
            --strength-medium: #F59E0B;
            --strength-strong: #16A34A;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--bg);
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            color: var(--ink);
            padding: 24px;
        }

        .card {
            width: 100%;
            max-width: 360px;
            background: #fff;
            border-radius: 16px;
            padding: 32px 28px 28px;
            box-shadow: 0 8px 30px rgba(17,24,39,0.08);
        }

        .logo {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 6px;
            margin-bottom: 20px;
        }

        .logo-mark {
            width: 44px;
            height: 44px;
            border: 2px solid var(--orange);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo-mark svg {
            width: 24px;
            height: 24px;
        }

        .logo-text {
            font-size: 20px;
            font-weight: 700;
            color: var(--ink);
        }

        .logo-text span {
            color: var(--orange);
        }

        h1 {
            font-size: 20px;
            font-weight: 700;
            text-align: center;
            margin: 0 0 6px;
        }

        .subtitle {
            text-align: center;
            color: var(--muted);
            font-size: 13px;
            margin: 0 0 24px;
        }

        label {
            display: block;
            font-size: 13px;
            color: var(--muted);
            margin-bottom: 6px;
        }

        .field {
            margin-bottom: 16px;
        }

        .input-wrap {
            position: relative;
            display: flex;
            align-items: center;
            border: 1.5px dashed var(--orange);
            border-radius: 10px;
            padding: 10px 12px;
            background: #fff;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        .input-wrap:focus-within {
            border-style: solid;
            box-shadow: 0 0 0 3px rgba(249, 115, 22, 0.15);
        }

        .input-wrap svg.lock {
            width: 16px;
            height: 16px;
            color: var(--orange-dark);
            margin-right: 8px;
            flex-shrink: 0;
        }

        .input-wrap input {
            border: none;
            outline: none;
            flex: 1;
            font-size: 14px;
            letter-spacing: 2px;
            color: var(--ink);
            background: transparent;
            width: 100%;
        }

        .toggle-eye {
            background: none;
            border: none;
            cursor: pointer;
            color: var(--muted);
            display: flex;
            align-items: center;
            padding: 0;
        }

        .toggle-eye svg {
            width: 18px;
            height: 18px;
        }

        .strength-row {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-top: 8px;
        }

        .strength-bar {
            flex: 1;
            height: 5px;
            border-radius: 3px;
            background: var(--border);
            overflow: hidden;
        }

        .strength-bar span {
            display: block;
            height: 100%;
            width: 0%;
            background: var(--border);
            border-radius: 3px;
            transition: width 0.3s ease, background-color 0.3s ease;
        }

        .strength-label {
            font-size: 12px;
            font-weight: 600;
            color: var(--muted);
            white-space: nowrap;
        }

        .submit-btn {
            width: 100%;
            padding: 14px;
            margin-top: 8px;
            border: none;
            border-radius: 24px;
            background: linear-gradient(180deg, var(--orange), var(--orange-dark));
            color: #fff;
            font-size: 15px;
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 6px 16px rgba(234,88,12,0.35);
            transition: transform .15s ease, box-shadow .15s ease;
        }

        .submit-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(234,88,12,0.4);
        }

        .hint {
            display: flex;
            gap: 8px;
            align-items: flex-start;
            margin-top: 18px;
            padding: 12px;
            background: #FFF7ED;
            border-radius: 10px;
            font-size: 12.5px;
            color: #9A5B12;
            line-height: 1.4;
        }

        .hint svg {
            width: 16px;
            height: 16px;
            flex-shrink: 0;
            margin-top: 1px;
            color: var(--orange-dark);
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12.5px;
            color: var(--muted);
        }

        .footer a {
            color: var(--orange-dark);
            text-decoration: none;
            font-weight: 600;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        /* ASP.NET MVC Validation styles */
        .text-danger, .field-validation-error {
            color: #EF4444;
            font-size: 12.5px;
            margin-top: 6px;
            display: block;
        }

        .validation-summary-errors {
            background-color: #FEF2F2;
            border: 1px solid #FEE2E2;
            border-radius: 10px;
            padding: 12px;
            margin-bottom: 16px;
            color: #991B1B;
            font-size: 13px;
        }

        .validation-summary-errors ul {
            margin: 0;
            padding-left: 16px;
        }
    </style>
</head>
<body>

<div class="card">
    <div class="logo">
        <div class="logo-mark">
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M4 20V4h3.2L16 15.5V4h4v16h-3.2L8 8.5V20H4z" fill="#F97316"/>
            </svg>
        </div>
        <div class="logo-text">Neme<span>Book</span></div>
    </div>

    <h1>Create a new password</h1>
    <p class="subtitle">Choose a strong password for your account.</p>

    <form asp-action="@formAction" asp-controller="Account" method="post" novalidate>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input asp-for="Token" type="hidden" />

        <div class="field">
            <label asp-for="Password">New password</label>
            <div class="input-wrap">
                <svg class="lock" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2a5 5 0 0 0-5 5v3H6a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8a2 2 0 0 0-2-2h-1V7a5 5 0 0 0-5-5zm-3 8V7a3 3 0 0 1 6 0v3H9z"/></svg>
                <input asp-for="Password" id="new-password" type="password">
                <button class="toggle-eye" type="button" aria-label="Show password">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z"/>
                        <circle cx="12" cy="12" r="3"/>
                    </svg>
                </button>
            </div>
            <span asp-validation-for="Password" class="text-danger"></span>
            <div class="strength-row">
                <div class="strength-bar"><span></span></div>
                <span class="strength-label">None</span>
            </div>
        </div>

        <div class="field">
            <label asp-for="ConfirmPassword">Confirm new password</label>
            <div class="input-wrap">
                <svg class="lock" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2a5 5 0 0 0-5 5v3H6a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8a2 2 0 0 0-2-2h-1V7a5 5 0 0 0-5-5zm-3 8V7a3 3 0 0 1 6 0v3H9z"/></svg>
                <input asp-for="ConfirmPassword" id="confirm-password" type="password">
                <button class="toggle-eye" type="button" aria-label="Show password">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z"/>
                        <circle cx="12" cy="12" r="3"/>
                    </svg>
                </button>
            </div>
            <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
        </div>

        <button class="submit-btn" type="submit">Confirm new password</button>
    </form>

    <div class="hint">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <path d="M9 12l2 2 4-4"/>
        </svg>
        <span>Your password should be at least 8 characters and include letters and numbers.</span>
    </div>

    <p class="footer">Remember your password? <a href="@Url.Action("Login", "Account")">Go back to log in</a></p>
</div>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
<script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>

<script>
    document.querySelectorAll('.toggle-eye').forEach(btn => {
        btn.addEventListener('click', function () {
            const input = this.parentElement.querySelector('input');
            const type = input.getAttribute('type') === 'password' ? 'text' : 'password';
            input.setAttribute('type', type);

            const svg = this.querySelector('svg');
            if (type === 'text') {
                this.style.color = 'var(--orange)';
                svg.innerHTML = '<path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></line>';
            } else {
                this.style.color = 'var(--muted)';
                svg.innerHTML = '<path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z"/><circle cx="12" cy="12" r="3"/>';
            }
        });
    });

    const passwordInput = document.getElementById('new-password');
    const strengthBarSpan = document.querySelector('.strength-bar span');
    const strengthLabel = document.querySelector('.strength-label');

    passwordInput.addEventListener('input', function () {
        const val = this.value;
        let strength = 0;
        let label = 'Very Weak';
        let color = 'var(--strength-weak)';
        let width = '10%';

        if (val.length === 0) {
            label = 'None';
            color = 'var(--border)';
            width = '0%';
        } else {
            if (val.length >= 8) strength += 1;
            if (/[A-Z]/.test(val)) strength += 1;
            if (/[a-z]/.test(val)) strength += 1;
            if (/[0-9]/.test(val)) strength += 1;
            if (/[^A-Za-z0-9]/.test(val)) strength += 1;

            if (val.length < 8) {
                label = 'Weak';
                color = '#EF4444';
                width = '30%';
            } else if (strength <= 2) {
                label = 'Weak';
                color = '#EF4444';
                width = '35%';
            } else if (strength === 3) {
                label = 'Medium';
                color = '#F59E0B';
                width = '60%';
            } else if (strength === 4) {
                label = 'Strong';
                color = '#16A34A';
                width = '85%';
            } else {
                label = 'Very Strong';
                color = '#15803D';
                width = '100%';
            }
        }

        strengthBarSpan.style.width = width;
        strengthBarSpan.style.backgroundColor = color;
        strengthLabel.textContent = label;
        strengthLabel.style.color = color;
    });

    // Trigger on initial load in case password field starts prefilled
    passwordInput.dispatchEvent(new Event('input'));
</script>
</body>
</html>
